package com.upSkill.java_6;

public class SabihaInterview {

	public static void main(String[] args) {
		/*   
		 * public class WithoutDuplicationString {
	
	public static void main(String[] args) {
		
		String word = "autoiuatioon";

        // Find unique characters
        String unique = removeDuplicates(word);

        // Print the result
        System.out.println("Original String: " + word);
        System.out.println("String without duplicates: " + unique);
    }

    public static String removeDuplicates(String str) {
        // Use HashSet to store unique characters
        HashSet<Character> charSet = new HashSet<>();
        StringBuilder result = new StringBuilder();

        // Iterate through the string
        for (char c : str.toCharArray()) {
            // Add only if the character is not already in the set
            if (charSet.add(c)) {
                result.append(c);
            }
        }

        return result.toString();
    }
		 * 
		 * 
		 * */
		
		
//		/////////////////////
		
		/*
		 * public class SortDecendingOrder {
	
	public static void main(String[] args) {
		 ArrayList<Integer> list = new ArrayList<>();
	        list.add(5);
	        list.add(1);
	        list.add(8);
	        list.add(3);

	        // Sort in descending order
	        Collections.sort(list, Collections.reverseOrder());

	        System.out.println(list);
	    }
		 * 
		 */
		 

	}

}
